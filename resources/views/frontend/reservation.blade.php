@extends('layouts.frontend')

@section('title', 'Reserve a Table — Bakers & Sweets')

@section('content')
    {{--
        Paste your existing Reservation page HTML here.

        @if(session('success'))
            <div class="alert-success">{{ session('success') }}</div>
        @endif

        <form action="{{ route('reservation.store') }}" method="POST">
            @csrf
            ... your existing reservation form fields ...
        </form>
    --}}
@endsection
